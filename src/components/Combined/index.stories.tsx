import React from "react";
import Combined from "./";

export default {
  title: "Combined",
  component: Combined,
};

export const Default = () => (
  <Combined>
    <p>Olá nova página</p>
  </Combined>
);
